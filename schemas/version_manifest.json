{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://ornithemc.net/mc-versions/schemas/version_manifest.json",
    "$defs": {
        "version": {
            "description": "A version in the Omniarchive format",
            "type": "string",
            "pattern": "^(((server-)?([abc]|inf?-|rd-|af-)?[0-9]+[.0-9]*)|[0-9]{2}w[0-9]{2}[a-z~])a?([_-][0-9a-z]+)*$"
        },
        "releaseType": {
            "description": "A release type (e.g. release, snapshot, etc.)",
            "type": "string",
            "pattern": "^[a-z_]+$",
            "examples": ["release", "snapshot", "old_alpha", "old_beta"]
        },
        "sha1": {
            "description": "An SHA-1 hash",
            "type": "string",
            "pattern": "^[0-9a-f]{40}$"
        }
    },
    "type": "object",
    "properties": {
        "$schema": {"type": "string", "format": "uri"},
        "latest": {
            "type": "object",
            "patternProperties": {
                "^[a-z_]+$": {"$ref": "#/$defs/version"}
            },
            "additionalProperties": false
        },
        "versions": {
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "id": {"$ref": "#/$defs/version"},
                    "type": {"$ref": "#/$defs/releaseType"},
                    "url": {
                        "type": "string",
                        "format": "uri-reference"
                    },
                    "sha1": {"$ref": "#/$defs/sha1"},
                    "time": {
                        "type": "string",
                        "format": "date-time",
                        "description": "The time at which the manifest referenced in `url` was generated"
                    },
                    "releaseTime": {
                        "type": "string",
                        "format": "date-time",
                        "description": "The time this version was released"
                    },
                    "details": {
                        "type": "string",
                        "format": "uri-reference"
                    },
                    "detailsSha1": {"$ref": "#/$defs/sha1"}
                },
                "required": ["id", "type", "url", "sha1", "time", "releaseTime", "details", "detailsSha1"]
            }
        },
        "additionalProperties": false
    }
}